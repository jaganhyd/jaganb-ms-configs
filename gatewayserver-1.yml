server:
  port: 8072
    
eureka: 
  instance:
    preferIpAddress: true #register with ip address instead of dns
  client: 
    fetchRegistry: true                  
    registerWithEureka: true
    serviceUrl: 
      defaultZone: http://localhost:8070/eureka/
